{
  "name": "uniclav-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot ./index.html",
    "build": "rm -rf dist && bun build ./index.html ./mentions-legales.html ./confidentialite.html ./cgu.html ./accessibilite.html --outdir=dist --minify --sourcemap=none && cp 404.html dist/ && touch dist/.nojekyll",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "typescript": "^5.9.0"
  }
}
